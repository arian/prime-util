"use strict";

// var rand = require('mout/random/randInt')

// function random(array){
//     return (array.length) ? array[rand(0, array.length - 1)] : null
// }

// module.exports = random

module.exports = require('mout/random/choice')
