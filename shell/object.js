/*
object methods
*/"use strict";

var object = require("prime/object")

object.extend({

    merge: require('../object/merge'),
    fromPath: require('../object/fromPath')

})

module.exports = object
